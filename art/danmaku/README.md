# Danmaku

Static bullet patterns, storyboards, and terminal-animation assets, including original patterns and spell-card homages.

Path format: `danmaku/<work-slug>/`. Put the representative static frame in `art.txt`; store animation frames as `frames/001.txt`, `002.txt`, and so on, all with the same canvas dimensions. Document frame rate and looping behavior in the work README.
